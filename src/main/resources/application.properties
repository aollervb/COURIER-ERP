spring.application.name=COURIER-ERP
logging.level.org.springframework.security=DEBUG

# Optional: initial password for first super-admin (default: changeme). Only used when no SUPER_ADMIN exists yet.
# courier.superadmin.initial-password=changeme

